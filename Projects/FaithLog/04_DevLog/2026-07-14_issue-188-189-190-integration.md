---
project: FaithLog
type: devlog
issues: ["#188", "#189", "#190"]
status: merged-develop
created: 2026-07-14
tags:
  - FaithLog
  - integration
  - billing
  - devotion
  - meal
  - concurrency
---

# #188/#189/#190 통합

## 통합 이력

- base: 최신 `origin/develop` `c7761da`
- branch: `integration/188-190-devotion-meal-billing`
- #188 `26bcc7f`, #189 `df94038`, #190 `bd9f604`를 각각 merge commit으로 병합해 TDD 이력을 보존했다.
- 문서 충돌은 세 기능의 승인 계약을 union으로 유지했고 Billing 충돌은 weekly bulk query, MEAL 전용 경로, source-key/상태 전이 잠금과 Devotion reopen을 함께 보존했다.

## 통합 리뷰 RED/GREEN

재오픈된 weekly row를 잠그지 않아 동시 0원 재제출 두 건이 모두 제출 검사를 통과할 수 있는 race를 결정적 latch 테스트로 재현했다. 최초 `1 test / 1 failure` 뒤 취소 재오픈과 재제출이 같은 weekly row의 `PESSIMISTIC_WRITE`를 사용하도록 수정했다. 신규 회귀와 #190 전체 재제출 통합 class는 GREEN이었다.

## 비Docker 검증

- 전체: `449 tests / 0 failures / 0 errors / 3 skipped`
- `./gradlew build`, `./gradlew asciidoctor`, `git diff --check`: 성공
- JaCoCo: line 94.41%, branch 75.77%, class 97.70%, method 89.79%
- test source 85개, 전체 Java source/test 646개
- REST Docs snippet group 151개, `index.adoc` 참조 150개 중 누락 0개

## Docker/Flyway/실제 HTTP QA

- 사용자 승인 정리 뒤 Docker Desktop 29.6.1 engine을 복구하고 격리 project `faithlog-qa-188-190-20260714`를 build/up했다.
- current source image: `sha256:c65cff97bdccd1bc88e1e06d5427e81b7951fce57cc68c750f36c10bffa94a04`
- PostgreSQL 17 clean DB는 V1→V8 8/8 success, 별도 upgrade DB는 V1→V7 target 뒤 V8만 적용해 8/8 success였다.
- PostgreSQL/Redis는 healthy/PONG, backend `GET /api/v1/health`는 200/UP이다.
- 실제 fixture `1783981848`, campus `1`에서 A/B/C/D 45 steps / failures 0을 기록했다. #188의 PAID+UNPAID 10,400원 합계·권한·non-Monday·2-sheet XLSX, #189의 다수 duty·계좌 격리·OPEN/SINGLE·optionIds·close 0 charge·10,000÷3=3,334/actual 10,002·재정산 409·8일 poll 30일 조회, #190의 관리자 PAID/terminal 409·cancel reopen·daily 보존·동일 row 재사용·0원 CANCELED 유지를 확인했다. 추가 fixture `rollback-1783982391`은 첫 MEAL charge write 뒤 두 번째 source-key 충돌 409에서 settlement/group/첫 charge 0건과 선행 충돌 row 1건을 확인해 실제 transaction rollback을 검증했다.
- cancel 직후 Excel에서 대상은 미제출 3-cell 행, 재제출 뒤 제출 11-cell 행으로 이동했다. 동일 charge에 PAID/CANCELED 실제 동시 요청은 200 1건/409 1건, daily 7/source row 1로 직렬화됐다.
- 최신 사용자 결정대로 compose down과 builder prune을 실행하지 않았고 backend `28080`, PostgreSQL `25432`, Redis `26379`를 계속 실행 중이다. 최종 host 가용 공간은 7.8GiB이며 데이터/volume/image 삭제는 0건이다.

## 프론트 계약 대조

read-only 확인한 frontend clean `develop` `aba1ab0`에는 #188 신규 weekly/export 호출, #189 MEAL enum·client, #190 관리자 PAID UI가 아직 연결되지 않았다. 기존 devotion GET/PUT, 본인 paid PATCH, legacy admin missing 호출은 일치한다. 프론트는 수정하지 않고 PM finding으로 보고한다.

## PR #191 CI 시간 정밀도 보강

Linux CI에서 생성 응답의 nanosecond `endsAt`과 DB 재조회 값의 정밀도 차이로 REST Docs 테스트 1건이 실패했다. 종료 API가 값을 변경한 회귀가 아니므로 생성 직후 DB 저장값을 기준으로 close 전후 불변을 비교하도록 테스트 한 줄을 보강했다. 실패 단일 테스트와 전체 `449 tests / 0 failures / 0 errors / 3 skipped`를 다시 통과했다.

## PM 완료 결과

- PR #191은 CI의 Spring Boot test/build, Docker image build, 협업 파일 검사를 통과한 뒤 squash 방식으로 `develop`에 병합됐다. merge commit은 `bb29e0176e33e527401c781a7854966e60dee9d5`다.
- frontend #188/#189/#190 연결 범위와 순서는 별도 작업으로 남는다.
- V8 settlement FK 조회 인덱스, MEAL 정산 반복 DB 접근, MEAL 청구 사용자 bulk lookup, #188 Billing projection port 경계는 후속 성능·아키텍처 후보로 남는다.
